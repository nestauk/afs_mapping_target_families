#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "afs_mapping_target_families")"
[ "$CONDA_DEFAULT_ENV" = "afs_mapping_target_families" ] || echo "Have you run \`make install\`?"
